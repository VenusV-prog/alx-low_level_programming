#include <stdio.h>

/**
 * main - Prints single digit numbers of base 10 from 0.
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 58; i < 68; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}

